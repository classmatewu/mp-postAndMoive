/* pages/welcome/welcome.wxss */
/**index.css**/
/* 小程序页面默认最外层有一个page元素包裹着 */
page {
  background-color: #b3d4db;
}
/* view容器的样式 */
.index-container{
  display: flex; /* flex布局 */
  flex-direction: column; /* 容器内列排布 */
  align-items: center; /* 容器内元素位于页面中轴线上 */
  margin-top: 200rpx;
}
/* 头像图片样式 */
.index-avatar {
  width: 200rpx;
  height: 200rpx;
}
/* 文本样式 */
.index-text {
  margin-top: 50rpx;
  font-weight: bold; /* 文本加粗 */
  font-family: MicroSoft Yahei; /*其实小程序的字体默认是跟操作系统有关的,Windows默认是微软雅黑,但我们最好也还是设置一下*/
}
/* 按钮样式 */
.index-button {
  padding: 20rpx;
  border: 1px solid #405f80;
  border-radius: 5px; /* 设置边框为圆角，注意这里用rpx为单位不生效，只能用px */
  margin-top: 200rpx;
}
